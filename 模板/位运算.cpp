#include <bits/stdc++.h>
using namespace std;
/************************************************************
* 位运算一定要注意运算符的优先级
* 
* & | ^ 的优先级是小于比较符号(!= == > <)的
*************************************************************/

// 异或
// 异或(⊕),相同为0， 不同为1
// a⊕a = 0
// 二进制中对于任意两个数都有 a+b=(a⊕b)+2⋅(a&b)
// a⊕b = (a & !b) | (!a & b)

// 或
// a | b >= max(a, b);

// (a & b) + (a | b) = a+b
int main()
{
    return 0;
}