#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define MOD 998244853
int main()
{
    ll n, m;
    cin >> n >> m;
    vector<ll> a(n), p(n);
    for(int i = 0; i < n ; i++)
    {
        cin >> a[i];
    }
    for(int i = 0; i < n ; i++)
    {
        cin >> p[i];
    }
    
    return 0;
}