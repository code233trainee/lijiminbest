#include <bits/stdc++.h>
using namespace std;
using ll = long long;
void solve()
{
    ll n, q;
    cin >> n >> q;
    vector<ll> vec(n);
    for(int i = 0; i < n; i++)
    {
        cin >> vec[i];
    }
    while(q--)
    {
        ll l, r;
        cin >> l >> r;
        l--;
        r--;
        
    }
}
int main()
{
    int t = 1;
    cin >> t;
    while(t--)
    {
        solve();
    }
    return 0;
}