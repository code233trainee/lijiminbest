#include <bits/stdc++.h>
using namespace std;
int main()
{
    string s;
    cin >> s;
    bool ok = false;
    while(true)
    {
        for(int i = 1; i < s.size() - 1; i++)
        {
            
        }
        if(ok)
        {
            break;
        }
    }
    return 0;
}